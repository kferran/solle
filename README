This site was a mess when a got a hold of it so I decided to build out the store as its own section/app.
All of the files for the store are include inside the store directory except for the image and fonts that are the root level.
